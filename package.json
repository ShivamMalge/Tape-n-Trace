{
  "name": "tape-n-trace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "type": "module",
  "description": "An interactive Theory of Computation workbench.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^3.0.5",
    "esbuild": "^0.28.2",
    "eslint": "^9.18.0",
    "ts-json-schema-generator": "^2.9.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=20.11"
  }
}
