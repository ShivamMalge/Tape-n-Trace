/**
 * A PDA drawn with the finite-automaton renderer — now shared with the
 * notebook widget from `@tape-n-trace/ui`; this module keeps the import path.
 */

export { pdaEdgeLabel, pdaToDrawable } from '@tape-n-trace/ui'
