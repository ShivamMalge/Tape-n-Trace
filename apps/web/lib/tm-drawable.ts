/**
 * A Turing machine drawn with the finite-automaton renderer — now shared with
 * the notebook widget from `@tape-n-trace/ui`; this module keeps the import path.
 */

export { tmEdgeLabel, tmToDrawable } from '@tape-n-trace/ui'
